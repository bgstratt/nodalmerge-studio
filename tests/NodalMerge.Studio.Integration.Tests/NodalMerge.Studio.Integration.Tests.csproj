<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.AspNetCore.TestHost" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="SourceGear.SQLite3" />
    <!-- plans/harness-hosting-architecture.md Phase C.4 (phase-c-implementation.md C3) — real MCP
         client, used to hit "/mcp-harness" the way the spawned CLI would (initialize + tools/call),
         instead of asserting on the endpoint's raw HTTP shape. -->
    <PackageReference Include="ModelContextProtocol" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NodalMerge.Studio.Host\NodalMerge.Studio.Host.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- docs/TREE_OBJECT_FORMAT.md golden vectors, consumed by CanonicalTreeSerializerVectorTests. -->
    <None Include="TestData\tree-format-vectors.v1.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
