# Web dashboard (out of v1 scope)

A web dashboard is explicitly **out of scope for v1** per the [architecture specification](../../docs/architecture/v1-architecture-spec.md).

Future iterations may provide administrative and visualization surfaces that consume the same MCP v1 contract and Studio Host APIs as the VS Code extension.

No code or build tooling exists in this folder yet.
